# Overview
- 11 cols, 9 subs.
- SELECT scores FROM WHERE id=123 (indexes id).
- Evaluate level + Charts (use libs).
- List top 10 group A (math, physics, chemistry) (pre-calculated in another table?).

- Responsive.
- Docker.
- Deploy.

# FE
- ReactJS + TailwindCSS + TS.
- TailAdmin?.

# BE
- NodeJS + Typescript.
- Validator (express-validator).
- OOP.
- Use ORM (Prisma).
- Use Postgre (Supabase).

# Deploy
- Some services like Heroku and Vercel has their own limits.

- Use Netlify for FE and Render for BE.


